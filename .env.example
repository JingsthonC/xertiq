# ===========================================
# XertiQ Production Environment Variables
# ===========================================
# Copy this file: cp .env.example .env
# Then fill in your actual values.

# ===== Git Repositories =====
FRONTEND_REPO=https://github.com/JingsthonC/xertiq_frontend.git
BACKEND_REPO=https://github.com/JingsthonC/xertiq_backend.git

# ===== Domain =====
DOMAIN=yourdomain.com
FRONTEND_URL=https://yourdomain.com
ADMIN_EMAIL=admin@yourdomain.com

# ===== Database (PostgreSQL) =====
# Docker Compose uses these to create the DB container AND build the connection URL
DB_USER=xertiq
DB_PASSWORD=CHANGE_ME_use_openssl_rand_base64_32
DB_NAME=xertiq_db
# Note: DATABASE_URL is constructed automatically in docker-compose.yml
# Format: postgresql://DB_USER:DB_PASSWORD@postgres:5432/DB_NAME

# ===== Authentication =====
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_64
JWT_EXPIRES_IN=7d

# ===== Solana Blockchain =====
# Use devnet for testing, mainnet-beta for production
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_PRIVATE_KEY=your_solana_wallet_private_key_base58

# ===== IPFS (Pinata) =====
PINATA_API_KEY=your_pinata_api_key
PINATA_SECRET_API_KEY=your_pinata_secret_key

# ===== Payments (optional — leave blank to disable) =====
STRIPE_SECRET_KEY=
PAYMONGO_SECRET_KEY=

# ===== Email (optional — leave blank to disable) =====
RESEND_API_KEY=
